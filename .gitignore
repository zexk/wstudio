.zig-cache/
zig-out/
zig-pkg/
result
result-*
dist/
.direnv/
*.wav
# ...but the shipped assets are committed: the default wavetable is embedded,
# and an SFZ in the library names its samples whatever the pack shipped.
!src/assets/wavetable/
!src/assets/wavetable/*.wav
!src/assets/library/**/*.wav
*~

# Local launch planning, not project documentation.
/docs/community-launch.md
/docs/reddit-posts.md
gui_screenshots/

# Build caches and local scratch, never project content.
.zig-global-cache/
/work/
